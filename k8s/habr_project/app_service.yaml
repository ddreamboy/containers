apiVersion: v1
kind: Service
metadata:
  name: habr-adapter-service
  labels:
    app: habr-adapter
spec:
  type: NodePort
  ports:
    - port: 7000
      targetPort: 7000
  selector:
    app: habr-adapter